node_modules
*.db
.env
.env.production
.netlify
dist
server/storage
_archive/

# Local scratch + tool logs (not part of the app)
tmp/
.firebase/logs/

# One-time Firebase export: real client PHI and staff records in plain JSON,
# plus every stored document's bytes. Keep it, back it up, never commit it.
migration/export/
